# Quick Start - Now UI PRO Flutter

## Prerequisites
- Flutter SDK >= 3.22.0
- Dart SDK >= 3.0.0
- Android Studio / Xcode (for emulators)
- VS Code or Android Studio with Flutter plugin

## Setup
```bash
flutter doctor          # Verify Flutter installation
flutter pub get         # Install dependencies
flutter run             # Run on connected device/emulator
```

## First Run
1. Install Flutter SDK 3.22+: https://flutter.dev/docs/get-started/install
2. Run `flutter doctor` to check for issues
3. Open project in IDE
4. Get dependencies: `flutter pub get`
5. Connect device or start emulator
6. Run: `flutter run`

## Development
```bash
flutter analyze    # Check for issues
flutter run        # Run with hot reload
```

## Important
- **Requires Flutter >= 3.22.0, Dart >= 3.0.0**
- Android/iOS emulator or physical device needed
- Hot reload available during development (press 'r')
- Null safety enabled
