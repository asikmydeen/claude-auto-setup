{%- if product.handle != blank -%}
  <button type="button" aria-label="Add to wishlist" class="btn btn-link text-dark" ct-button-wishlist data-product-handle="{{ product.handle }}">
    <i class="fa fa-heart text-sm"></i>
  </button>
{%- endif -%}